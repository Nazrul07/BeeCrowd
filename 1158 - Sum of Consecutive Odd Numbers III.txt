#include<stdio.h>
int main()
{
    int test,i,x,y,j,sum=0,count=0;
    scanf("%d",&test);
    for(i=1 ; i<=test ; i++)
    {
        scanf("%d%d",&x,&y);
        while(count!=y)
        {
            if(x%2!=0)
            {
                sum=sum+x;
                count++;
            }
        x++;
        }
        printf("%d\n",sum);
        sum=0;
        count=0;
    }
    return 0;
}

#include<stdio.h>
int main()
{
    int x,count=0,sum=0;
    float avg;
    scanf("%d",&x);
    while(x>0)
    {
        sum=sum+x;
        scanf("%d",&x);
        count++;
    }
    avg=(float)sum/count;
    printf("%.2f\n",avg);
    return 0;
}
